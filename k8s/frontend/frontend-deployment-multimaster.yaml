apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: apc-fe-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: frontend
        image: 192.168.0.169/kdh/alphacar-frontend:2.0.10
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: API_URL
          value: "https://alphacar.cloud/api"
        - name: PORT
          value: "8000"
        - name: NEXT_PUBLIC_BASE_URL
          value: "https://alphacar.cloud"
        - name: NEXT_PUBLIC_DOMAIN
          value: "https://alphacar.cloud"
        - name: NODE_EXTRA_CA_CERTS
          value: "/etc/ssl/certs/selfsigned.crt"
        volumeMounts:
        - name: ssl-cert
          mountPath: /etc/ssl/certs/selfsigned.crt
          subPath: selfsigned.crt
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "500Mi"
            cpu: "500m"
      volumes:
      - name: ssl-cert
        secret:
          secretName: ssl-cert
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: apc-fe-ns
spec:
  selector:
    app: frontend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP

